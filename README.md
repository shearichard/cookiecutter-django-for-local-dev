# cookiecutter template for local Django dev

A cookiecutter template to create Django projects, deliberately limited in scope but including the packages I tend to always use in Django projects . Primarily useful for locally run projects.
